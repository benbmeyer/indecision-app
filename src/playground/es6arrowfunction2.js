const add = function (a, b) {
    return a + b;
};

const add = (, b) => {
    
}


console.log(add(55, 1));

const user = {
    name: 'Ben';
    cities: ['Minneapolis, St. Paul, St. Cloud'],
    printPlacesLived: function () {
        this.name
        this.cities

        this.cities.forEach(city) {
            console.log(this.name + ' has lived in ' + city)
        });
    }
};